#include <iostream>

int main() {
    int integer; // defines a 32 bit signed (i.e. positive or negative) integer
    unsigned int posInteger; // defines a 32 bit unsigned integer (only positive and can use all the 32 bits for data storage)
    char x; // 8 bits (1 byte)
    short y; // 2 bytes
    long z; // 4 bytes
    long long u; // 8 bytes
    float x = 5.5f; // 4 bytes decimal value (must add f to end of value to make it a float)
    double y; // 8 bytes decimal value
    bool variable = true; // 1 byte with 0 being assigned to false and any other vaolue being true
}