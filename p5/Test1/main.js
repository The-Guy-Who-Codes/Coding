function setup() {
    createCanvas(600,600);
    background(125,125,125);
}

function draw() {
}

function bar(x, y) {
    //y == to on or off status
    //x == to verticial(1) or horizontal(-1)
    if(y === 1){
        fill("red");
    }
}
